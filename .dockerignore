# Git & metadata
.git
.gitignore

# Python environments & caches
.venv
venv
__pycache__
*.pyc
*.pyo
*.pyd
*.pkl
*.log

# Test / tooling caches
.pytest_cache
.mypy_cache
ruff_cache

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode
.idea

# Jupyter artifacts
.ipynb_checkpoints
notebooks/

# Docker files (optional – they’re not needed inside the image)
Dockerfile
docker-compose.yml

# === Project-specific data ===

# All extraction data (raw + curated) - not needed for serving
extraction/data/

# Processed / feature CSVs - training artifacts only
core/data/processed/
core/data/features/